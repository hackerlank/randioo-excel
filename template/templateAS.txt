package com.youzu.topsango.config {
	import flash.utils.ByteArray;
	import flash.utils.Dictionary;

	public class $CLASS_NAME{
		public static final String urlKey="$URL";
		$PROP	
		static public function parse($BYTES_VAR_NAME:ByteArray):$DIC_TYPE{
			var dic:$DIC_TYPE=new $DIC_TYPE();
			while($BYTES_VAR_NAME.hasRemaining()){
				var $PO:$CLASS_NAME = new $CLASS_NAME();
				$ASSIGN
				dic$DIC_ADD
			}
			return dic;
		}
}
