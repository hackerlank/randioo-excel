package com.aim.config.fo;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;

import com.aim.config.cache.$CLASS_NAMECache;

public class $CLASS_NAME{
	public static String urlKey="$URL";
	$PROP	
	public static void parse(ByteBuffer $BYTES_VAR_NAME){
		$BYTES_VAR_NAME.order(ByteOrder.LITTLE_ENDIAN);
		while($BYTES_VAR_NAME.hasRemaining()){
			byte[] b = null;
			$CLASS_NAME $PO = new $CLASS_NAME();
			$ASSIGN
			$CLASS_NAMECache.putConfig($PO);
		}
	}
}
